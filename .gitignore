node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.local
.DS_Store

# Expo-generated native build dirs (we don't store these); the
# top-level `ios/` folder holds our native SwiftUI source and is
# intentionally tracked.
android/

# Xcode build artefacts
ios/build/
ios/DerivedData/
ios/Marko.xcodeproj/
ios/*.xcworkspace/

# Local API keys (BYOK lives in the Keychain at runtime; the xcconfig
# is only a developer convenience for build-time defaults).
ios/Marko/Resources/Secrets.xcconfig
